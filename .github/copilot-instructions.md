# Grocery Inventory Management

- Frontend: Vite + React JavaScript in `frontend/`
- Backend: Flask REST API in `backend/`
- Current storage: in-memory seed data; keep persistence behind API/repository boundaries for the future MySQL migration.
- Run the backend from `backend/` with `python app.py`.
- Run the frontend from `frontend/` with `npm run dev`.
